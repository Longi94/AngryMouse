<Window x:Class="AngryMouse.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AngryMouse"
        mc:Ignorable="d"
        Title="AboutWindow"
        Height="Auto"
        Width="Auto"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Name="AppName" FontSize="24" HorizontalAlignment="Center" />
        <Label Grid.Row="1" Name="AppVersion" HorizontalAlignment="Center" />
        <Label Grid.Row="2" Name="AppCopyright" HorizontalAlignment="Center" />
        <Button Grid.Row="3" Content="Github" Click="Github_OnClick"/>
    </Grid>
</Window>
